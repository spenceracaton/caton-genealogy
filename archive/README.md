# Archive

This directory is provenance, not active research context. It contains
superseded pass notes and original external imports after their usable findings
were reconciled into `BRIEF.md`, `claims.jsonl`, and `evidence/`.

Read an archived file only to reconstruct a specific decision. Do not use it as
an independent source of current facts.
